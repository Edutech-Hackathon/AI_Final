# 📚 문해력 향상 AI 튜터

학생들의 문해력 향상을 위한 초개인화 뉴스/지문 요약 및 퀴즈 생성 도구

## 🎯 주요 기능

- **난이도별 텍스트 요약**: 초등/중등/고등 수준에 맞춘 맞춤형 설명
- **쉬운 비유와 예시**: 복잡한 내용을 일상적인 비유로 설명
- **즉석 퀴즈 생성**: OX 퀴즈와 객관식 문제로 이해도 확인
- **학습 기록 추적**: 세션별 학습 진행 상황 모니터링

## 🚀 설치 및 실행

### 1. 환경 설정
```bash
# 가상환경 생성 (선택사항)
conda create -n edutech python=3.11.13 
conda activate edutech

# 패키지 설치
pip install -r requirements.txt
```

### 2. API 키 설정
`.env.example` 파일을 복사하여 `.env` 파일을 만들고, OpenAI API 키를 입력하세요:
```
OPENAI_API_KEY=해당 API키
```

### 3. 실행
```bash
streamlit run app.py
```

## 📁 프로젝트 구조

```
literacy-tutor/
├── app.py                 # 메인 Streamlit 애플리케이션
├── utils/
│   ├── __init__.py
│   ├── text_processor.py  # 텍스트 처리 및 요약 모듈
│   ├── quiz_generator.py  # 퀴즈 생성 모듈
│   └── prompts.py         # 프롬프트 템플릿 관리
├── config.py              # 설정 관리
├── .env                   # API 키 (git ignore)
├── .env.example           # API 키 템플릿
├── .gitignore
├── requirements.txt       # 의존성 패키지
└── README.md
```

## 🎨 사용 방법

1. **텍스트 입력**: 어려운 뉴스 기사나 교과서 내용을 붙여넣기
2. **난이도 선택**: 학습자 수준 선택 (초등/중등/고등)
3. **요약 생성**: AI가 선택한 수준에 맞게 내용을 쉽게 설명
4. **퀴즈 풀기**: 생성된 퀴즈로 이해도 확인
5. **피드백 확인**: 정답과 해설 확인

## 🔧 커스터마이징

`config.py`에서 다음 설정들을 변경할 수 있습니다:
- 모델 선택 (gpt-4o-mini, gpt-3.5-turbo 등)
- 최대 토큰 수
- 온도(Temperature) 설정
- 퀴즈 문제 개수
